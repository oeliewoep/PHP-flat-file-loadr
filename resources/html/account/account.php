<div class="hmargin">

<div class="wf">
<h2>Welkom terug</h2>
<h3>Profiel wijzigen</h3>
<pre>
<?php
/*
print_r($_POST);
if(isset($_POST['password']))
{
    // sanitize input
    $username = $_POST['username'];
    $password = $_POST['password'];
    
    if(empty($username))
    $username = 'unknown';

    printf("Dear user with username: '%s',\r\n",$username);
    
    $hash = create_hash($password);
    echo "password: ".$_POST['password']."\r\n";
    echo "hash: ".$hash."\r\n";
    $pwdcheck = validate_password($password,$hash);
    echo "pwdcheck: ".$pwdcheck."\r\n";
    if (!preg_match('/^(?=[a-z])(?=[A-Z])[a-zA-Z]{8,}$/',$password))
    {
        echo "Password does not meet minimum requirements. \r\n";
    }
}
*/
?>
</pre>
</div>

</div>